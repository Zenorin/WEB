interface:
  display_name: "Project Backend API"
  short_description: "Own API entrypoints, error envelopes, persistence boundary, authz, and backend test matrix."
  default_prompt: "$project-backend-api"

policy:
  allow_implicit_invocation: true
